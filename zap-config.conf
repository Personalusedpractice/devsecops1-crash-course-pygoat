# Set the context
contexts.context1.name=Default Context
contexts.context1.urls.include=https://ginandjuice.shop.*

# Set authentication details if required
auth.method=manual
auth.username=admin
auth.password=admin

# Other configuration options
